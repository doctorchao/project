<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>笔记本</title>
    <link rel="stylesheet" href="../stylesheets/common.css">
    <link rel="stylesheet" href="../stylesheets/reset.css">
    <link rel="stylesheet" href="../stylesheets/index.css">
</head>
<body>
    <div class="header">
        <div class="w1170 wrap">
            <h1>云笔记</h1>
            <div class="head-btn">
                <form action=""> 
                    <button type="submit" formaction="/write">写笔记</button>
                </form>
            </div>
        </div>
    </div>
    <div class="middle-box">
        <div class="login-box">
            <form>
                <h1>新用户注册</h1>
                <div class="item">
                    <input type="text" placeholder="账号">
                </div>
                <div class="item">
                    <input type="text" placeholder="邮箱">
                </div>
                <div class="item">
                    <input type="password" placeholder="密码">
                </div>
                <div class="item">
                    <button id="abc" class="item-btn btn1"><span>注册</span></button>
                </div>
            </form>
        </div>
    </div>
</body>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<!-- <script src="../public/js/register.js"></script> -->
<script>
var arr = document.getElementsByTagName("input")
var abc = document.querySelector(".btn1")
abc.onclick = function(){
    axios.post("/registe",{
        name:arr[0].value,
        email:arr[1].value,
        password:arr[2].value
    }).then( (res) =>{
        console.log(res.data)
        if(res.data.code==200){
            alert(res.data.msg)
            top.location.href = "http://localhost:3000"
        }else{
            alert(res.data.msg)
        }
    })
}
</script>
</html>